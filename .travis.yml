language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kirillyershov/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kirillyershov/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "docker-react"
  env: "DockerReact-env"
  bucket_name: "elasticbeanstalk-us-east-2-577720079767"
  bucket_path: "docker-react"
  on:
    branch: master
